export type Profile = {
    _id: string;
    fullName: string;
    headline: string;
    // profileImage {alt, "image": asset->url},
    // shortBio,
    // location,
    // fullBio,
    // email,
    // "resumeURL": resumeURL.asset->url,
    // socialLinks,
    // skills
}